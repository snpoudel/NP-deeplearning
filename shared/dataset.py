"""Dataset   for NP-deeplearning.

This should first have standarization script and save the scaler. Let's only standardize input and not target variable.
Then this should have a nice dataset class that provides sequence data for model training and evaluation to then be used to downstream models.
Since all downstream models are sequence models, one preprocessing dataset class should be sufficient for all models.
"""
